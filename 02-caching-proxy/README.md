# Caching Proxy Server Implementation

<https://roadmap.sh/projects/caching-server>

## Usage

node index.js --port portnumber --origin url

```bash

node index.js --port 3000 --origin https://dummyjson.com

```
